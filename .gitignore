# Secrets & local data
.env
ig_pulse.db
*.db
*.db-journal
logs/
exports/
registry.db
registry.db-journal
data/

# Raw API fixtures contain real audience comments/usernames (R7) — don't commit.
# (Sanitized sample fixtures, if any, can be force-added explicitly.)
tests/fixtures/*.json
tests/fixtures/*.txt

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# OS / editor
.DS_Store

# Dev artifacts
.playwright-mcp/
ig-pulse-*.png
